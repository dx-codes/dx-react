import render, { _update, invokeComponentUpdate, createPortal, hooksUpdater } from './render'

const ReactDOM = {
  render,
  invokeComponentUpdate,
  _update,
  createPortal,
  hooksUpdater,
}

export default ReactDOM